import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818220000_apf_ai_traceability_governance.sql"),"utf8");const fn=readFileSync(resolve("supabase/functions/apf-dossier-semantic-suggestions/index.ts"),"utf8");describe("APF semantic traceability governance",()=>{it("authorizes review and reserves usage",()=>{expect(fn).toContain("has_apf_dossier_permission");expect(fn).toContain("reserve_ai_usage");expect(fn).toContain("finalize_ai_usage");});it("uses configured provider and outbound allowlist",()=>{expect(fn).toContain("ai_providers");expect(fn).toContain("get_ai_provider_key_by_id");expect(fn).toContain("assertSafeOutboundUrl");});it("persists only service-side without resetting reviews",()=>{expect(sql).toContain("auth.role()<>'service_role'");expect(sql).toContain("status='pending'");expect(sql).toContain(",method,confidence,rationale)values");expect(sql).toContain(",'ai',greatest");});});
