import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818140000_apf_billing_bridge.sql"),"utf8");describe("APF billing bridge",()=>{it("submits approved net PF once",()=>{expect(sql).toContain("batch_id uuid not null unique");expect(sql).toContain("greatest(b.total_pf-b.disputed_pf,0)");expect(sql).toContain("not in('approved','glosa_resolved','closed')");});it("keeps finance linkage governed",()=>{expect(sql).toContain("assert_backoffice_staff");expect(sql).toContain("billing_record_id");expect(sql).toContain("backoffice_audit_log");});it("is tenant safe and append audited",()=>{expect(sql).toContain("is_organization_member");expect(sql).toContain("apf_measurement_billing_events");});});
