import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/tests/database/21_apf_dossier_rbac_isolation.test.sql"),"utf8");
describe("APF database isolation suite",()=>{it("exercises two tenants through authenticated RLS",()=>{expect(sql).toContain("set local role authenticated");expect(sql).toContain("RLS exposes only tenant A dossier");expect(sql).toContain("tenant A cannot view tenant B dossier");});it("denies cross-tenant export and writes",()=>{expect(sql).toContain("cross-tenant export is denied");expect(sql).toContain("cross-tenant direct write is denied");expect(sql).toContain("'42501'");});it("always rolls fixtures back",()=>{expect(sql.trimEnd()).toMatch(/rollback;$/);});});
