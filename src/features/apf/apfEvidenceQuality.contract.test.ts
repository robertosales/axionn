import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818120000_apf_evidence_quality.sql"),"utf8");describe("APF evidence quality",()=>{it("scores objective provenance guarantees",()=>{for(const value of ["verified","has_hash","has_permanent_url","has_repository","has_provenance"])expect(sql).toContain(value);});it("detects HU versus implementation contradictions",()=>{expect(sql).toContain("criterion_claim_without_verified_evidence");expect(sql).toContain("implementation_without_requirement");});it("is tenant safe and feeds audit findings",()=>{expect(sql).toContain("apf_can_access_dossier");expect(sql).toContain("apf_audit_findings");});});
