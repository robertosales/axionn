import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818110000_apf_exception_reviews.sql"),"utf8");describe("APF exception reviews",()=>{it("covers every proposal disposition",()=>{for(const value of ["counted","absorbed","reuse_zero_pf","not_countable","non_functional","pending_evidence","hu_implementation_divergence","audit_risk"])expect(sql).toContain(`'${value}'`);});it("requires a valid absorption target",()=>{expect(sql).toContain("p_disposition='absorbed'");expect(sql).toContain("id<>p_counting_item_id");});it("is tenant safe and auditable",()=>{expect(sql).toContain("apf_can_access_dossier");expect(sql).toContain("apf_dossier_events");});});
