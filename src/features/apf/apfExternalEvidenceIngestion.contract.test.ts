import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818160000_apf_external_evidence_ingestion.sql"),"utf8");describe("APF external evidence ingestion",()=>{it("supports the phase 4 providers",()=>{for(const provider of["github","jira","azure_devops","redmine"])expect(sql).toContain(`'${provider}'`);});it("is idempotent and immutable-safe",()=>{expect(sql).toContain("unique(dossier_id,provider,external_ref)");expect(sql).toContain("status not in('homologated','superseded','cancelled')");});it("does not trust connector evidence automatically",()=>{expect(sql).toContain("'unverified'");expect(sql).toContain("content_hash");expect(sql).toContain("apf_dossier_events");});});
