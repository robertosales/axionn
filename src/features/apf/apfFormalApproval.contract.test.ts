import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818270000_apf_formal_batch_approval.sql"),"utf8");describe("APF formal batch approval",()=>{it("separates submission and approval permissions",()=>{expect(sql).toContain("apf.dossier.validate");expect(sql.match(/apf\.dossier\.homologate/g)?.length).toBeGreaterThanOrEqual(2);});it("requires an approver distinct from the batch creator",()=>{expect(sql).toContain("b.created_by=auth.uid()");expect(sql).toContain("aprovador formal deve ser diferente");});it("protects billing behind formal approval",()=>{expect(sql).toContain("submit_apf_batch_for_billing");expect(sql).toContain("b.status not in('approved','glosa_resolved','closed')");});});
