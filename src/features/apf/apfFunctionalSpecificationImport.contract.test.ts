import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818210000_apf_functional_specification_import.sql"),"utf8");describe("APF functional specification import",()=>{it("requires collection and review permissions",()=>{expect(sql).toContain("apf.dossier.collect_evidence");expect(sql).toContain("apf.dossier.review");});it("preserves source text and hash",()=>{expect(sql).toContain("original_text");expect(sql).toContain("p_content_hash");expect(sql).toContain("functional_specification");});it("is bounded and idempotent",()=>{expect(sql).toContain("2097152");expect(sql).toContain("200");expect(sql).toContain("content_hash=p_content_hash");});});
