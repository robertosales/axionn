import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818180000_apf_git_provider_adapter.sql"),"utf8");describe("APF Git provider adapter",()=>{it("supports synchronized providers",()=>{for(const p of["gitlab","github","azure_devops"])expect(sql).toContain(`'${p}'`);});it("delegates to the proven importer",()=>{expect(sql).toContain("import_apf_git_evidence");expect(sql).toContain("apf_can_access_dossier");});it("preserves the actual provider",()=>{expect(sql).toContain("jsonb_set");expect(sql).toContain("gi.provider");});});
