import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818150000_apf_governance_metrics.sql"),"utf8");describe("APF governance metrics",()=>{it("isolates dashboard by tenant",()=>{expect(sql).toContain("is_organization_member");expect(sql).toContain("organization_id=p_organization_id");});it("measures human accuracy and audit risk",()=>{expect(sql).toContain("suggestion_acceptance_rate");expect(sql).toContain("critical_open_findings");});it("measures glosa and counting divergence",()=>{expect(sql).toContain("glosa_rate");expect(sql).toContain("counting_divergence_pf");});});
