import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818200000_apf_granular_permissions.sql"),"utf8");describe("APF granular permissions",()=>{it("registers every proposed capability",()=>{for(const p of["view","create","collect_evidence","review","validate","homologate","export","manage_templates"])expect(sql).toContain(`apf.dossier.${p}`);});it("requires tenant membership and role mapping",()=>{expect(sql).toContain("is_organization_member");expect(sql).toContain("role_permissions");expect(sql).toContain("team_members");});it("guards writes independently from RLS",()=>{expect(sql).toContain("apf_enforce_record_permission");expect(sql).toContain("before insert or update or delete");expect(sql).toContain("errcode='42501'");});});
