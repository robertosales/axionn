import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818190000_apf_jira_evidence_adapter.sql"),"utf8");describe("APF Jira adapter",()=>{it("accepts writes only from the connector",()=>{expect(sql).toContain("auth.role()<>'service_role'");expect(sql).toContain("grant execute on function public.upsert_apf_jira_issue_link");});it("links issues to a tenant HU",()=>{expect(sql).toContain("user_story_id");expect(sql).toContain("t.org_id=p_organization_id");expect(sql).toContain("unique(organization_id,external_ref)");});it("imports through governed ingestion",()=>{expect(sql).toContain("import_apf_external_evidence");expect(sql).toContain("content_hash");expect(sql).toContain("permanent_url");});});
