import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818100000_apf_logical_file_matrix.sql"),"utf8");
describe("APF logical file matrix",()=>{it("records the five decision dimensions",()=>{for(const field of ["recognizable","maintained_by_application","independent_lifecycle","inside_boundary","used_by_transaction"])expect(sql).toContain(field);});it("supports ALI AIE and rejection",()=>{expect(sql).toContain("'ALI','AIE','not_logical_file','pending'");});it("requires tenant, official item and rationale",()=>{expect(sql).toContain("apf_can_access_dossier");expect(sql).toContain("session_id=v_dossier.counting_session_id");expect(sql).toContain("nullif(trim(p_justification),'')");});});
