import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818280000_apf_manual_evidence_provenance.sql"),"utf8");const service=readFileSync(resolve("src/features/apf/services/apfEvidenceDossier.service.ts"),"utf8");const document=readFileSync(resolve("src/features/apf/utils/apfDossierMarkdown.ts"),"utf8");describe("APF manual evidence governance",()=>{it("requires provenance and justification at database boundary",()=>{expect(sql).toContain("current_user='authenticated'");expect(sql).toContain("manual_evidence");expect(sql).toContain("Justificativa da evidência manual é obrigatória");});it("persists and renders author date and rationale",()=>{expect(service).toContain("justification: input.justification.trim()");expect(document).toContain("e.collectedBy");expect(document).toContain("e.metadata?.justification");});});
