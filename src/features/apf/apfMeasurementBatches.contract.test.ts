import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818130000_apf_measurement_batches.sql"),"utf8");describe("APF measurement batches",()=>{it("only consolidates homologated immutable dossiers",()=>{expect(sql).toContain("status='homologated'");expect(sql).toContain("content_hash");expect(sql).toContain("pf_snapshot");});it("supports approval glosa and closing workflow",()=>{for(const state of ["under_review","approved","glosa_requested","glosa_resolved","closed"])expect(sql).toContain(`'${state}'`);});it("is tenant safe and append-audited",()=>{expect(sql).toContain("is_organization_member");expect(sql).toContain("apf_measurement_batch_decisions");});});
