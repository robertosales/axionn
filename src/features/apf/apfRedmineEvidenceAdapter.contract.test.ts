import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818170000_apf_redmine_evidence_adapter.sql"),"utf8");describe("APF Redmine adapter",()=>{it("imports only tenant-scoped active links",()=>{expect(sql).toContain("i.organization_id=d.organization_id");expect(sql).toContain("i.is_active");expect(sql).toContain("l.organization_id=d.organization_id");});it("uses the linked user story",()=>{expect(sql).toContain("l.axionn_entity_type='user_story'");expect(sql).toContain("l.axionn_entity_id=d.user_story_id");});it("delegates to governed ingestion",()=>{expect(sql).toContain("import_apf_external_evidence");expect(sql).toContain("content_hash");expect(sql).toContain("permanent_url");});});
