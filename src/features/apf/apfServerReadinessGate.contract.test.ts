import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const sql=readFileSync(resolve("supabase/migrations/20260818290000_apf_server_readiness_gate.sql"),"utf8");describe("APF server readiness gate",()=>{it("checks every homologation prerequisite in the transaction",()=>{for(const rule of["all_acceptance_criteria_require_decision","positive_criterion_requires_confirmed_verified_evidence","counted_items_require_validation_and_evidence","counting_overrides_require_justification","calculation_memory_does_not_close","ruleset_and_counting_session_must_be_frozen"])expect(sql).toContain(rule);});it("verifies document hash and validate permission server-side",()=>{expect(sql).toContain("digest(convert_to(p_rendered_markdown,'UTF8'),'sha256')");expect(sql).toContain("apf.dossier.validate");});it("writes the immutable version only after all gates",()=>{expect(sql.indexOf("insert into public.apf_dossier_versions")).toBeGreaterThan(sql.indexOf("calculation_memory_does_not_close"));});});
