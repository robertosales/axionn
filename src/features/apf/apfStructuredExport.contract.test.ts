import{readFileSync}from"node:fs";import{resolve}from"node:path";import{describe,expect,it}from"vitest";const migration=readFileSync(resolve("supabase/migrations/20260818250000_apf_export_audit_events.sql"),"utf8");const service=readFileSync(resolve("src/features/apf/services/apfEvidenceDossier.service.ts"),"utf8");const component=readFileSync(resolve("src/features/apf/components/dossier/ApfDossierValidation.tsx"),"utf8");
describe("APF structured export",()=>{it("exports the persisted snapshot instead of recalculating",()=>{expect(service).toContain("snapshot_json");expect(component).toContain("version.snapshotJson");expect(component).toContain("downloadJsonAsFile");});it("audits dossier and batch exports",()=>{expect(migration.match(/'exported'/g)?.length).toBe(2);expect(migration).toContain("measurement_batch");expect(migration).toContain("apf.dossier.export");});});
