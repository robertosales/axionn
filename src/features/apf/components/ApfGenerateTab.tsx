import { ApfDossierWorkspace } from "./dossier/ApfDossierWorkspace";

/** Operational entry point for the APF evidence dossier workflow. */
export function ApfGenerateTab() {
  return <ApfDossierWorkspace />;
}
