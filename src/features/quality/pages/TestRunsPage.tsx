import{PlayCircle}from"lucide-react";import{Link,Navigate}from"react-router-dom";import{Badge}from"@/components/ui/badge";import{Card,CardContent}from"@/components/ui/card";import{Button}from"@/components/ui/button";import{useOrganization}from"@/contexts/OrganizationContext";import{QUALITY_MANAGEMENT_ENABLED}from"@/lib/featureFlags";import{useTestRuns}from"../hooks/useTestRuns";
export default function TestRunsPage(){const{currentOrganizationId}=useOrganization();const q=useTestRuns(currentOrganizationId);if(!QUALITY_MANAGEMENT_ENABLED)return<Navigate to="/sala-agil/dashboard" replace/>;return<main className="mx-auto max-w-[1300px] space-y-6 p-4 md:p-8"><header className="border-b pb-6"><p className="mb-2 flex items-center gap-2 text-sm font-medium text-primary"><PlayCircle className="h-4 w-4"/>Quality Intelligence</p><h1 className="text-3xl font-bold">Execuções</h1><p className="text-sm text-muted-foreground">Acompanhe sessões manuais e seus resultados congelados.</p></header>{q.isLoading?<p className="py-12 text-center text-muted-foreground">Carregando execuções…</p>:q.data?.length?<div className="space-y-3">{q.data.map(r=><Card key={r.id}><CardContent className="flex flex-col gap-3 p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><h2 className="font-semibold">{r.name}</h2><Badge variant={r.status==="completed"?"secondary":"outline"}>{r.status}</Badge></div><p className="mt-1 text-xs text-muted-foreground">{r.environment_name||"Ambiente não informado"} · {new Date(r.created_at).toLocaleString("pt-BR")}</p></div><Button asChild variant="outline"><Link to={`/sala-agil/qualidade/execucoes/${r.id}`}>Abrir execução</Link></Button></CardContent></Card>)}</div>:<div className="rounded-xl border border-dashed p-12 text-center"><PlayCircle className="mx-auto mb-3 h-10 w-10 text-muted-foreground"/><p className="font-medium">Nenhuma execução criada</p><p className="text-sm text-muted-foreground">Crie uma execução a partir de um plano.</p></div>}</main>}
